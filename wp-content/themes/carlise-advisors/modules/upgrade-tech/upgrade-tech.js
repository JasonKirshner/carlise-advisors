/**
 * Initializes the javascript for the upgrade-tech module
 * @param {Object} el - The DOM Node containing the data-module="upgrade-tech" attribute.
 */

const upgradeTech = (el) => {
  console.warn('initializing upgradeTech module')
}

export default upgradeTech
