/**
* Initializes the site's pagination module.
* @constructor
* @param {Object} el - The site's pagination container element.
*/
function pagination (el) {
  this.el = el
}

export default pagination
