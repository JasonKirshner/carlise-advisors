/**
 * Initializes the javascript for the copy-grid module
 * @param {Object} el - The DOM Node containing the data-module="copy-grid" attribute.
 */

const copyGrid = (el) => {
  console.warn('initializing copyGrid module')
}

export default copyGrid
